TS_SEARCH_PATHS=(
  "$HOME/dev/personal"
  "$HOME/dev/work"
  "$HOME/.config"
  "$HOME/.codex/worktrees:2"
  "$HOME/.local/share/opencode/worktree/"
  "$HOME"
)

TS_WINDOW_COMMANDS=(
  "nvim"
  "opencode2"
  ""
  ""
)
